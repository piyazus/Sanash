# Sanas — сценарий живой демонстрации прототипа

Длительность: 7–8 минут показа + вопросы. Аудитория: Innoforce/Avtobys,
акимат, менторы. Технический уровень: нулевой, все термины объясняем.

Железный узел ещё не собран, поэтому демо — это прогон алгоритма на
записях из настоящего автобуса + схемы будущего железа. Так и говорить,
не изображать наличие устройства.

## Что подготовить заранее (чек-лист)

1. **Лог прогона валидации.** Прогон `scripts/run_door_apc.py` на
   локальной подвыборке данных (~0,43 ГБ). Заранее проверить, что
   подвыборка на диске и скрипт отрабатывает; если живой запуск
   невозможен (нет данных/времени), показать сохранённый вывод прогона.
   Не запускать скачивание данных во время демо.
2. **Таблица результатов**: `paper/results/apc_validation.md` — открыть
   или распечатать раздел "Headline table".
3. **График "счётчик против истины"** (кривая числа людей в салоне N_t по
   времени: наш счётчик рядом с истинным значением). Данные для графика
   лежат в `paper/results/apc_validation.json` (ряд N_t с шагом 1 с +
   таблица по эпизодам). **Фигуру строит CADy** — запросить у CADy готовый
   PNG/SVG заранее; на момент написания скрипта файл фигуры не создан.
4. **Схемы из `docs/hardware/figures/`**, открыть в браузере:
   - `system_architecture.svg` — общая схема системы;
   - `sensor_placement.svg` — размещение датчика над дверью;
   - `dataflow.svg` и `wiring_schematic.svg` — держать в запасе для
     технических вопросов, в основной показ не включать.
5. Если есть макет экрана Avtobys с уровнем загруженности — показать его;
   если макета нет (на момент написания его нет), описать словами, ничего
   не рисовать на ходу как "готовый интерфейс".

## Ход демонстрации

### 1. Проблема (30 секунд)

Сказать: "Вы стоите на остановке. В приложении видно, где автобус, но не
видно, влезете ли вы в него. Мы добавляем в Avtobys уровень
загруженности салона — 5 ступеней, от свободно до битком."

### 2. Как устроена система (1 минута) — system_architecture.svg

Показать схему, вести пальцем по потоку: датчик над дверью → бортовой
блок → сервер Avtobys → приложение.

Сказать: "Над каждой дверью датчик-дальномер, как парктроник, 64 луча.
Вошёл человек — плюс один, вышел — минус один, на конечной сброс в ноль.
Это не камера: существует только 64 числа-расстояния, изображения нет
физически."

Обязательно: показать на пунктирную часть схемы (камера в салоне) и
сказать: "Пунктир — это будущая Фаза 2, она спроектирована, но не
сделана. Всё сплошное — Фаза 1, о ней сегодня и говорим."

### 3. Где стоит датчик (1 минута) — sensor_placement.svg

Показать вид сбоку и сверху: датчик над дверным проёмом смотрит
вертикально вниз, линия счёта — в тамбуре.

### 4. Прогон алгоритма (2 минуты) — лог run_door_apc.py

Сказать: "Своего автобуса у нас пока нет, поэтому мы взяли открытый
научный датасет, записанный в настоящем городском автобусе в Германии,
просимулировали на нём наш датчик и прогнали весь алгоритм счёта."

Показать лог: события посадки/высадки с направлением и итоговый счёт
салона. Не листать быстро — дать увидеть, что события ловятся по одному,
с направлением.

### 5. Результат против истины (1–2 минуты) — график от CADy + таблица

Показать график N_t: "Синяя линия — наш счётчик, вторая — истинное число
людей в салоне. Средняя ошибка — 0,17 человека, к концу получаса прогона
счётчик разошёлся с истиной на одного человека."

Показать таблицу из apc_validation.md: "Поймано 24 из 25 посадок и
высадок, направление верно во всех пойманных, ноль ложных срабатываний
на отрезках без событий."

### 6. Честные ограничения (1 минута) — сказать до того, как спросят

- "Это 25 событий. Первая валидация, не сертификация. Одно событие — это
  4 процентных пункта."
- "Запись велась камерой сбоку; наш датчик будет смотреть строго вниз —
  это стандартная геометрия промышленных счётчиков, но её ещё предстоит
  подтвердить на железе."
- "Единственный пропуск — два человека, вышедшие вплотную друг за другом.
  Это известный сложный случай для всех счётчиков."
- "Датчик пока был в симуляции; настоящий чип проверим на столе — это
  ближайший шаг."

### 7. Просьба (30 секунд, по аудитории)

- Innoforce/Avtobys: доступ к автобусу, контакт по API, вместимость
  моделей автобусов, LOI.
- Акимат: содействие в пилоте, контактное лицо.
- Менторы: [нужен источник: SANASH Master Document, раздел 9 — шаблон
  письма с конкретным техническим вопросом; в репозитории отсутствует,
  не импровизировать].

## Подготовка к вопросам

**"Так какая точность?"** — "24 из 25 на первой валидации, но выборка в
25 событий не позволяет заявлять процент как характеристику продукта.
Отраслевой стандарт приёмки (VDV 457) требует тысяч событий. В известном
полевом исследовании вендор заявлял 98%, а замер в эксплуатации показал
53–55% — поэтому мы называем только то, что измерили, и точность продукта
измерим на пилоте."

**"Почему не камера, она же точнее?"** — "Камеру в салоне мы проверили
первой — по глубине она измеримо не работает (видит 10% пола, связь с
числом людей почти нулевая). А дверной датчик снимает вопрос приватности
полностью: изображения не существует. Камера остаётся в плане Фазы 2 как
поправка счёта, после отдельного разрешения."

**"Сколько это стоит?"** — "Класс цены датчика — единицы долларов за чип
при объёме. Подтверждённой сметы комплекта на автобус пока нет
[нужна реальная цифра], перечень компонентов открыт, посчитаем к пилоту."

**"Когда пилот / когда в продакшн?"** — "Следующий шаг — сборка узла на
столе, затем пилот. Сроки зависят от доступа к автобусу — это и есть наша
просьба сегодня. Дат не называем, пока нет доступа."

**"А счётчик не накапливает ошибку?"** — "Накапливает, поэтому на каждой
конечной он сбрасывается в ноль по GPS. На длинных маршрутах в Фазе 2
планируется поправка от камеры — это план, не готовая функция."

**"Это следит за людьми?"** — "Нет. 64 числа-расстояния, из них нельзя
восстановить ни лицо, ни одежду. Система знает только, сколько людей в
салоне, и не знает, кто они."

## Чего НЕ делать в демо

- Не называть "96%" без слов "25 событий, первая валидация".
- Не показывать Фазу 2 как готовую функцию; на схемах она пунктиром.
- Не называть цен и сроков — их нет.
- Не обещать детекцию вандализма/безопасности: это отложенная идея, не
  функция продукта.
- Не выдавать симуляцию датчика за работу реального чипа.
