"""Minimal reproducible counting baseline on DISCO.

This package is the "simplest reproducible baseline end-to-end" of
GROUND_TRUTH.md section 11 step 5. It is deliberately dumb. It does not select
an architecture for the product, it does not define the five occupancy levels,
and it is not trained on bus cabin data, because none exists yet.
"""
